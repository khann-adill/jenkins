pipeline {
   agent any
   parameters {
       string(defaultValue: '1.0', description: 'Provide the required version', name: 'version')
   }
	stages {
      stage('shell') {
         steps {
            sh 'echo $version'
		}
	}
}
}
